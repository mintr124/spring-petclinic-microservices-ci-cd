apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "vets-service.fullname" . }}
  labels:
    app: {{ include "vets-service.name" . }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      app: {{ include "vets-service.name" . }}
  template:
    metadata:
      labels:
        app: {{ include "vets-service.name" . }}
    spec:
      containers:
        - name: {{ .Chart.Name }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          ports:
            - containerPort: 8080
          env:
            - name: SPRING_PROFILES_ACTIVE
              value: "docker,mysql"
            - name: SPRING_APPLICATION_NAME
              value: {{ .Values.service.name | quote }}
            - name: SPRING_CLOUD_DISCOVERY_ENABLED
              value: "true"
            - name: SPRING_CLOUD_EUREKA_CLIENT_REGISTER_WITH_EUREKA
              value: "true"
            - name: SPRING_CLOUD_EUREKA_CLIENT_FETCH_REGISTRY
              value: "true"
            - name: SPRING_CLOUD_EUREKA_CLIENT_SERVICEURL_DEFAULTZONE
              value: http://discovery-server:8761/eureka/
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
