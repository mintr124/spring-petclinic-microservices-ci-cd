replicaCount: 1

image:
  repository: mintr124/config-server   # thay tên repo cho từng service
  tag: "main"
  pullPolicy: IfNotPresent

service:
  name: "config-server"
  type: NodePort
  port: 8888

resources: {}

nodeSelector: {}

tolerations: []

affinity: {}
